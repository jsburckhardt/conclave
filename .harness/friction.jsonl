{"timestamp":"2026-06-20T00:07:00Z","message":"No explicit 'clean' script in package.json — inferred rm -rf dist node_modules as the clean behavior from build output directory and standard npm conventions."}
{"timestamp":"2026-06-20T00:07:00Z","message":"No explicit 'boot' script in package.json — inferred npm ci as the boot command from package-lock.json presence and verification.yml install step."}
{"timestamp":"2026-06-20T00:07:00Z","message":"Lint verb combines three separate checks (eslint, prettier, typecheck) — inferred from verification.yml listing them as separate steps that all belong to code quality."}
{"timestamp":"2026-06-20T08:13:19Z","message":"boot/npm ci fails: package-lock.json out of sync with package.json (@emnapi optional deps drift); using 'npm install' to obtain node_modules, then restoring committed lockfile to avoid unrelated churn. KEY_QUESTION: should the committed package-lock.json be regenerated to match package.json?"}
{"timestamp":"2026-06-20T08:33:03Z","message":"harness boot (npm ci) fails: package-lock.json out of sync with package.json (missing/invalid transitive @emnapi/* optional deps). Used 'npm install' to sync the lockfile so node_modules could be installed for the verification gate."}
{"timestamp":"2026-06-20T08:50:36Z","message":"harness test/verify wrap 'npm test' (vitest run WITHOUT --coverage), so the >=80% coverage gate required by issue #6 AC/TP-14 is NOT enforced by the harness. Verified separately via 'npm run test:coverage' (now green: 85.7/85.4/82.4/85.7). Baseline test:coverage was pre-existing red (70/79/65/70)."}
{"timestamp":"2026-06-20T08:51:27Z","message":"Issue #4: ./harness test and ./harness verify run 'vitest run' WITHOUT --coverage, so the configured 80% coverage thresholds (vitest.config.ts) are not enforced by the harness. There is no 'harness coverage' verb. Had to run 'npm run test:coverage' directly to prove the >=80% gate (lines/branches/functions/statements). Suggest adding a coverage verb or having test pass --coverage."}
{"timestamp":"2026-06-21T01:00:01Z","message":"Issue #7: ./harness test and ./harness verify wrap 'npm test' (vitest run WITHOUT --coverage), so the >=80% lines/branches/functions/statements thresholds in vitest.config.ts are NOT enforced by the harness and there is no 'harness coverage' verb. Had to run 'npm run test:coverage' directly to prove the gate (now green: 96.45/90.8/100/96.44 with src/cli.ts excluded; add-member.ts, cli-program.ts, and resolveCouncilConfigPath all exercised). Suggest a 'harness coverage' verb or having test pass --coverage."}
{"timestamp":"2026-06-22T09:50:00Z","message":"harness verify/test reports pass/fail but does not surface coverage %; ran 'npm run test:coverage' directly to confirm the >=80% gate (CORE-COMPONENT-0009) for #3 — 96.16% stmts / 89.41% branches."}
